{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy to GitHub",
            "type": "shell",
            "command": "git add . && git commit -m 'Update content' && git push origin main",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}